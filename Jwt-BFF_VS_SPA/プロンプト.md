## 質問01
- 標準を定義しているIETFの上げているアーキテクチャパターンを全て図にして欲しい
- 得るものが実在するケースを調べて考えてほしい
  - 特に今回我々が作成する基幹システムには外部からAPIが呼び出される要件がある
  - その外部とは同じK8のPod内に存在する別のベンダーが作成するWebアプリケーションの想定になる
  - 別のベンダーが作成するWebアプリケーションにはそこから外部の人間にアカウントを払い出す想定もあるらしい
  - まずは我々が作成するアプリケーションの2つ、マスタデータと会計周りのトランザクション管理のサービスのみを考えた認証認可方式をかんがえる必要がある
  - プラスで外部からのAPI呼び出しなどをどう想定するかをかんがえる必要がある、これもJWTを使ったものになる？その場合は外部が作るWEBアプリの認証も我々と同じものを使う必要がある？
- 以下の認識であっているか
  - NextjsのServerActionsでトークンが露出するかどうかは認証認可の形式には関わってこない、要はセッションIDを渡されようと、JWTをそのまま渡されようと同じこと、JWTを渡される場合は、トークンを保存する機構がないんだから原理的にクライアントにトークンを渡すしかない認識であっている？
  - その場合は署名検証のみになるからステートレスで可用性が高くなる？
    - ここは自分の理解ができていないから原理を解説して図にしてほしい

## 質問02
- Redisを使わずにJWTのトークンそのものをクライアントにわたすことが自体が危険だという認識がある
  - これについて認識をただしたい
  - JWTはそれ自体が完結した証明であり、公開鍵がキャッシュされている間はすべて検証が通る
  - クライアントにJWTをHttpOnlyCookieとして入れ込めば、XSSでも持ち出すことはできずに攻撃されることはない、いやそうするとCSRFみたいな別の脅威の可能性が出てくるのか？
  - ここらへんをもっと詳細に解説してほしい、図を入れてほしい、IETFのパターン2になるってことかな？、パターン3もか？
- 2の我々の基幹システムについて
  - 社内のユーザーがベンダーのアプリを使うときもこの構成は変わらない認識であっているか？
    - スタンスは我々のAPIを呼ぶ以上、我々のAPIが署名検証できるJWTをもってこい、それはこのIdPだ（Cognit）みたいなイメージ
  - (a) システム間認証のみ、(b) ユーザー連携（フェデレーション）、(c) Token Exchange（RFC 8693）の違いが正確に理解できていない、詳細に解説して図でも表現してほしい
  - 現状外部の人間だけがベンダーのアプリを使っているがそうではない、社内のユーザーは我々のシステムも使うし、別ベンダーのアプリも使う
  - その中で別ベンダーのアプリは我々のリソースサーバーからデータを取得するイメージ
  - つまり、外部のベンダーのアプリからのAPI呼び出しは我々のシステムからするとそれが払い出したユーザーなのか社内のユーザーなのかみたいなのは知る必要がないということ？それは監査的にまずいのか？
    - そもそも監査的にまずいとかはどこを調べればわかる？業務システムのIETF的なサイトがあるの？

## 質問03
- → 今回の結論は変わらず1a。理由は前回同様「会計・基幹システムでは即時失効（退職者無効化・インシデント時の全セッション破棄）が事実上の要件」だから。1bは「Redisを置けない・置きたくない環境での正当な次善」であり、「危険な構成」ではない——この位置づけの訂正が今回の答え。
  - こいつがわからない、1bでもexpまで有効なだけでそれって設定した時間でしょ？5分とか15分とか
  - これもだめなの？
  - あとdenylistが何を行っているのかわからない
  - 1が両方IEFTが定めているBackend For Frontend — BFFに準拠していることはわかった
    - でもなんでクライアントにHttp-OnlyだとしてもJWTのトークンを渡しているのに、これがトークン本体は別のサーバー、Redisなどにおいて、引換券だけをクライアントに持たせる構成が同じ安全度になってるのかがわからん、どう考えても危ない気がする、私の歪んだ認知を説得してただしてほしい
- そもそも我々は基幹システムのマスタデータ管理DB、会計のトランザクション、それを参照、更新する各サイト、それらを提供します、他ベンダーが作るコアシステムにもAPIとしてデータは提供するけどどう使うかはそっちが勝手にやってね、みたいなスタンス
  - でも最近認証基盤を統一したいみたいな話が上がっている
  - その時に我々のスタンスをどう持っていくかをかんがえる必要がある
  - aのシステム間認証のみ、を当初想定していた、我々のAPIを使うんだから我々の認証トークン持ってこいよと
  - 外部ベンダーはそっちで認証なりなんなり勝手にやれよみたいな
  - でもこの方式だといまのcognitをIdPとしてユーザーの証跡を追うみたいなのは理論上不可能ってことだよね？じゃあそもそも要件段階でユーザーの証跡をどこまで追えるようにするべきかみたいなのが整理されていないからだめって認識であってる？
  - 結局どういういうスタンスで行くべきなのだ？
  - おそらくBが必要、でも我々は当初Aの想定、Bだと全ての認証認可の責務を我々が追うことになるので追加要件だよ
    - このスタンスか？

## 質問04
- 1について
  - Redisセッションを持たずにexpを設定するということは、expが切れるまで自由にBFFへの全権をもたせることを意味する
  - expを5分など短くすれば、それは結局失効の確認をする機構がひつよう、それはRedis
  - じゃあRedisを持たずに暗号化JWtCookie方式を採用するときはどういうケースなの？無制限にexpを設定するってこと？
  - 現状用途が全くわからないし、Redis使うしかないんじゃない？
  - 今回のケースと他のケースも調べて解説してほしい、図も欲しい
- 2について
  - スタンスについて大いに納得した
  - 今までの調査内容と経緯を把握したうえで現状と今後の展望の画を書きたい
  - 持っていく資料をそれぞれ作成したい
    - お客さん、別ベンダー、同じ会社のインフラ担当、我々アプリケーションチーム

## 質問05
- 1bが実務で成立しているカラクリは理解できた
  - つまりこれはSPAでも同じことができるわけだ
  - ブラウザには暗号化クッキーをもたせる、expを数分に設定して、状態はIdPに外注する
    - BFFはIdPにexpをチェックするAPIを叩くことになるんだよね、じゃあそのAPI自体はIdP，今回であればCognitが用意するのか
  - じゃあこれはメジャーな構成の認識であっているか？そのばあいはexpは何を基準に決められるのか
  - この構成になったバージョンの画も欲しい
    - アプリケーションチームとインフラチームにまず今我々が考えている構成として展開したい、それは1bと1a両方提示して、即時無効化など監査の要件によってどっちか変わる、即時無効化が必要な場合、expを許容できない場合はRedisが必要になる、みたいな感じであってる？
- 目指すアーキテクチャについて
  - 論点➀のNoバージョンのアーキテクチャの画も欲しい
  - これが現状我々が考えているアーキテクチャになるかな？でも要件で足りなさそうなところがあって、ホントにこれで良いんだっけ？みたいな話に持っていくべきか？
  - そうなると認証を共通化したいみたいな話にたいして我々のスタンスをケースごとにかんがえる必要があるからそこも整理してほしい
- ロードマップについて
  - これもっと詳細にしてほしい、それぞれのフェーズごとにアーキテクチャの画が欲しい